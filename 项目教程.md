# Calendar 日历提醒应用 - 完整教程

## 📋 目录

1. [项目简介](#项目简介)
2. [功能特性](#功能特性)
3. [项目结构](#项目结构)
4. [环境配置](#环境配置)
5. [核心功能实现](#核心功能实现)
6. [使用指南](#使用指南)
7. [开发指南](#开发指南)
8. [常见问题](#常见问题)
9. [技术栈](#技术栈)

---

## 📱 项目简介

**Calendar** 是一款功能完整的 Android 日历提醒应用，支持：
- 📅 日历日期选择
- ⏰ 提醒创建和管理
- 🔔 系统通知提醒
- 📝 提醒历史记录
- 🎨 Material Design 3 界面

---

## ✨ 功能特性

### 1. 日历功能
- **日期选择**：点击日历选择日期
- **年月快速切换**：点击年份/月份快速跳转
- **当前日期高亮**：自动高亮显示今天的日期
- **提醒日期标记**：显示哪些日期有提醒

### 2. 提醒管理
- **创建提醒**：添加标题、内容、开始/结束时间
- **编辑提醒**：修改已有提醒信息
- **完成提醒**：标记提醒为已完成
- **删除提醒**：删除不需要的提醒
- **恢复提醒**：从历史中恢复已删除的提醒

### 3. 系统通知
- **提醒开关**：可选择是否启用系统提醒
- **提前提醒**：设置提前 0-60 分钟提醒
- **到点响铃**：提醒时间到达时自动响铃
- **通知显示**：显示提醒标题和内容

### 4. 数据管理
- **本地存储**：使用 SharedPreferences 存储数据
- **JSON 序列化**：使用 Gson 进行数据序列化
- **数据持久化**：应用重启后数据不丢失

---

## 📁 项目结构

```
Calendar/
├── app/
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/example/myapplication/
│   │   │   │   ├── MainActivity.java              # 主活动（日历界面）
│   │   │   │   ├── RemindersListActivity.java     # 提醒列表活动
│   │   │   │   ├── Reminder.java                  # 提醒数据模型
│   │   │   │   ├── ReminderManager.java           # 提醒数据管理
│   │   │   │   ├── ReminderAdapter.java           # 提醒列表适配器
│   │   │   │   ├── AlarmHelper.java               # 提醒设置工具
│   │   │   │   ├── AlarmReceiver.java             # 提醒接收器
│   │   │   │   ├── NotificationHelper.java        # 通知管理
│   │   │   │   └── AlarmDebugHelper.java          # 调试工具
│   │   │   ├── res/
│   │   │   │   ├── layout/
│   │   │   │   │   ├── activity_main.xml          # 主界面布局
│   │   │   │   │   ├── activity_reminders_list.xml # 提醒列表布局
│   │   │   │   │   ├── dialog_reminder.xml        # 提醒对话框布局
│   │   │   │   │   ├── item_reminder.xml          # 提醒项布局
│   │   │   │   │   └── dialog_year_month_picker.xml # 年月选择对话框
│   │   │   │   ├── values/
│   │   │   │   │   ├── strings.xml                # 字符串资源
│   │   │   │   │   └── themes.xml                 # 主题样式
│   │   │   │   └── drawable/
│   │   │   │       ├── reminder_dot.xml           # 提醒标记图标
│   │   │   │       └── selected_date_indicator.xml # 选中日期指示器
│   │   │   └── AndroidManifest.xml                # 应用清单文件
│   │   └── build.gradle.kts                      # 应用构建配置
│   └── build.gradle.kts                          # 项目构建配置
└── gradle/                                        # Gradle 配置
    └── libs.versions.toml                        # 依赖版本管理
```

---

## 🛠️ 环境配置

### 1. 系统要求
- **Android Studio**：最新版本（推荐 Hedgehog 或更高）
- **JDK**：JDK 17 或更高
- **Android SDK**：API 24 (Android 7.0) 或更高
- **Gradle**：8.0 或更高

### 2. 导入项目

1. **打开 Android Studio**
2. **选择 "Open"** 或 "File → Open"
3. **选择项目文件夹** `C:\Users\62490\Desktop\Calendar`
4. **等待 Gradle 同步完成**

### 3. 配置 SDK

1. **打开 File → Project Structure**
2. **设置 Compile SDK Version**：34 或更高
3. **设置 Min SDK Version**：24
4. **设置 Target SDK Version**：34

### 4. 同步依赖

项目会自动下载以下依赖：
- **Material Components**：Material Design 3 组件
- **Gson**：JSON 序列化库
- **AndroidX Libraries**：Android 扩展库

---

## 💻 核心功能实现

### 1. 提醒数据模型（Reminder.java）

```java
public class Reminder implements Serializable {
    private final String id;                    // 唯一标识符
    private final String date;                  // 日期（格式：yyyy-MM-dd）
    private String title;                       // 标题
    private String content;                     // 内容
    private String startTime;                   // 开始时间（格式：HH:mm）
    private String endTime;                     // 结束时间（格式：HH:mm）
    private long timestamp;                     // 时间戳
    private boolean isCompleted;                // 是否完成
    private boolean isDeleted;                  // 是否删除
    private boolean enableNotification;         // 是否启用通知
    private int notificationMinutesBefore;      // 提前提醒分钟数
}
```

**关键点**：
- 实现 `Serializable` 接口，支持序列化
- 使用 `final` 修饰 `id` 和 `date`，确保不可变
- 提供完整的 getter/setter 方法

### 2. 数据管理（ReminderManager.java）

**功能**：
- 保存提醒到 SharedPreferences
- 使用 Gson 进行 JSON 序列化
- 提供查询、删除、更新等方法

**核心方法**：
```java
// 保存提醒
public void saveReminder(Reminder reminder)

// 获取所有提醒
public List<Reminder> getAllReminders()

// 获取活动提醒（未完成、未删除）
public List<Reminder> getActiveReminders()

// 获取已完成提醒
public List<Reminder> getCompletedReminders()

// 标记为已完成
public void markAsCompleted(String reminderId)

// 删除提醒
public void deleteReminder(String reminderId)

// 恢复提醒
public void restoreReminder(String reminderId)
```

### 3. 系统提醒功能

#### 3.1 提醒设置（AlarmHelper.java）

**功能**：设置和取消系统提醒

**核心方法**：
```java
public static void setAlarm(Context context, Reminder reminder) {
    // 1. 解析日期和时间
    // 2. 计算提醒时间（开始时间 - 提前分钟数）
    // 3. 检查时间是否已过期
    // 4. 创建 PendingIntent
    // 5. 使用 AlarmManager 设置提醒
}

public static void cancelAlarm(Context context, Reminder reminder) {
    // 取消已设置的提醒
}
```

**时间计算示例**：
```
提醒日期：2025-01-15
开始时间：14:30
提前时间：5 分钟
→ 提醒时间：2025-01-15 14:25
```

#### 3.2 提醒接收（AlarmReceiver.java）

**功能**：接收 AlarmManager 的广播，显示通知

**工作流程**：
```
AlarmManager 触发
    ↓
发送广播到 AlarmReceiver
    ↓
AlarmReceiver.onReceive()
    ↓
加载提醒数据
    ↓
检查提醒是否有效
    ↓
调用 NotificationHelper 显示通知
```

#### 3.3 通知管理（NotificationHelper.java）

**功能**：创建通知渠道和显示通知

**关键代码**：
```java
// 创建通知渠道（Android 8.0+ 必需）
public static void createNotificationChannel(Context context) {
    NotificationChannel channel = new NotificationChannel(
        CHANNEL_ID,
        "Reminder Notifications",
        NotificationManager.IMPORTANCE_HIGH  // 高优先级，会响铃
    );
    channel.enableVibration(true);  // 启用震动
    channel.enableLights(true);     // 启用指示灯
    channel.setSound(soundUri, null); // 设置声音
}

// 显示通知
public static void showReminderNotification(Context context, Reminder reminder) {
    // 构建通知
    // 设置标题、内容、图标
    // 显示通知
}
```

### 4. 主界面（MainActivity.java）

**主要功能**：
- 显示日历视图
- 处理日期选择
- 显示当前日期和时间
- 添加/编辑提醒对话框
- 更新提醒指示器

**关键方法**：
```java
// 初始化视图和数据
private void initViews()
private void initData()

// 设置日历
private void setupCalendar()

// 显示提醒对话框
private void showReminderDialog()
private void showReminderDialogInternal(Reminder reminder)

// 更新提醒指示器
private void updateReminderIndicator()

// 高亮今天日期
private void highlightToday()

// 恢复所有提醒
private void restoreAllAlarms()
```

### 5. 提醒列表（RemindersListActivity.java）

**主要功能**：
- 显示所有活动提醒
- 显示历史提醒
- 编辑、删除、完成提醒
- 清除历史记录

**视图切换**：
- **活动提醒视图**：显示未完成的提醒
- **历史视图**：显示已完成或已删除的提醒

---

## 📖 使用指南

### 1. 添加提醒

1. **选择日期**
   - 在日历上点击要添加提醒的日期
   - 或点击年份/月份快速跳转

2. **点击"Add Reminder"按钮**

3. **填写提醒信息**
   - **标题**：提醒的标题（可选）
   - **内容**：提醒的详细内容（可选）
   - **开始时间**：点击时间选择器选择开始时间
   - **结束时间**：点击时间选择器选择结束时间

4. **设置系统提醒**（可选）
   - 打开"🔔 Enable System Reminder"开关
   - 选择提前提醒时间（0、5、10、15、30、60 分钟）

5. **保存**
   - 点击"Save"按钮保存提醒

### 2. 查看提醒

1. **在主界面查看**
   - 日历下方会显示有提醒的日期列表
   - 格式：`Dates with reminders: Jan 15, Jan 20, Jan 25`

2. **查看所有提醒**
   - 点击"View All Reminders"按钮
   - 进入提醒列表界面

### 3. 编辑提醒

1. **在提醒列表中**，点击提醒项的"Edit"按钮
2. **修改信息**后点击"Save"

### 4. 完成提醒

1. **在提醒列表中**，点击提醒项的"Complete"按钮
2. 提醒会被标记为已完成，并移到历史记录

### 5. 删除提醒

1. **在提醒列表中**，点击提醒项的"Delete"按钮
2. **确认删除**后，提醒会被标记为已删除

### 6. 恢复提醒

1. **切换到历史视图**（点击"View History"）
2. **点击提醒项的"Restore"按钮**
3. 提醒会恢复到活动列表

### 7. 清除历史

1. **切换到历史视图**
2. **点击"Clear All"按钮**
3. **确认清除**后，所有历史记录会被永久删除

---

## 🔧 开发指南

### 1. 添加新功能

#### 示例：添加提醒优先级

1. **修改 Reminder.java**
```java
private int priority; // 1=低, 2=中, 3=高

public int getPriority() { return priority; }
public void setPriority(int priority) { this.priority = priority; }
```

2. **修改对话框布局**
```xml
<Spinner
    android:id="@+id/prioritySpinner"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" />
```

3. **在 MainActivity 中处理**
```java
Spinner prioritySpinner = dialogView.findViewById(R.id.prioritySpinner);
// 设置适配器和监听器
```

### 2. 自定义通知样式

修改 `NotificationHelper.java`：
```java
NotificationCompat.Builder builder = new NotificationCompat.Builder(context, CHANNEL_ID)
    .setSmallIcon(R.drawable.custom_icon)  // 自定义图标
    .setLargeIcon(bitmap)                 // 大图标
    .setStyle(new NotificationCompat.BigPictureStyle()  // 大图样式
        .bigPicture(bitmap))
    .setColor(Color.RED);                  // 通知颜色
```

### 3. 添加数据导出功能

```java
public void exportReminders(Context context) {
    List<Reminder> reminders = getAllReminders();
    String json = gson.toJson(reminders);
    
    // 保存到文件
    File file = new File(context.getExternalFilesDir(null), "reminders.json");
    FileWriter writer = new FileWriter(file);
    writer.write(json);
    writer.close();
}
```

### 4. 添加数据导入功能

```java
public void importReminders(Context context, File file) {
    FileReader reader = new FileReader(file);
    String json = reader.read();
    
    Type type = new TypeToken<List<Reminder>>() {}.getType();
    List<Reminder> reminders = gson.fromJson(json, type);
    
    // 保存到 SharedPreferences
    for (Reminder reminder : reminders) {
        saveReminder(reminder);
    }
}
```

---

## ❓ 常见问题

### Q1: 提醒没有触发？

**可能原因**：
1. 权限未授予（Android 13+ 需要通知权限）
2. 提醒时间已过期
3. 设备处于省电模式

**解决方法**：
1. **检查权限**：
   - 设置 → 应用 → Calendar → 通知 → 允许
   - 设置 → 应用 → Calendar → 权限 → 使用精确提醒 → 开启

2. **检查时间**：
   - 确保提醒时间设置为未来时间
   - 检查提前时间设置是否合理

3. **检查设备设置**：
   - 关闭省电模式
   - 将应用加入后台运行白名单

### Q2: 应用重启后提醒失效？

**原因**：应用重启后 AlarmManager 中的提醒会丢失

**解决方法**：
- 代码中已在 `MainActivity.onCreate()` 中调用 `restoreAllAlarms()`
- 确保应用启动时正确恢复了提醒

### Q3: 如何调试提醒功能？

**使用调试工具**：
```java
// 在 MainActivity.onCreate() 中添加
AlarmDebugHelper.debugAllReminders(this);  // 查看所有提醒状态
AlarmDebugHelper.testAlarm(this);          // 测试提醒（1分钟后触发）
```

**查看日志**：
- 在 Android Studio 的 Logcat 中过滤：
  - `AlarmHelper`：提醒设置日志
  - `AlarmReceiver`：提醒触发日志
  - `NotificationHelper`：通知显示日志

### Q4: 如何修改提醒提前时间选项？

**修改 MainActivity.java**：
```java
private void showNotificationTimePicker(TextInputEditText timeEdit) {
    // 修改选项数组
    String[] options = {"0", "5", "10", "15", "30", "60", "120"}; // 添加 120 分钟
    // ...
}
```

### Q5: 如何自定义通知声音？

**修改 NotificationHelper.java**：
```java
// 使用自定义声音文件
Uri soundUri = Uri.parse("android.resource://" + 
    context.getPackageName() + "/" + R.raw.custom_sound);
channel.setSound(soundUri, null);
```

---

## 🛠️ 技术栈

### 核心库
- **Material Components**：Material Design 3 UI 组件
- **Gson**：JSON 序列化/反序列化
- **AndroidX**：Android 扩展库

### Android API
- **AlarmManager**：系统提醒管理
- **BroadcastReceiver**：接收系统广播
- **NotificationManager**：通知管理
- **SharedPreferences**：本地数据存储
- **CalendarView**：日历视图

### 设计模式
- **单例模式**：ReminderManager
- **适配器模式**：ReminderAdapter
- **观察者模式**：事件监听

---

## 📝 代码规范

### 1. 命名规范
- **类名**：大驼峰（PascalCase），如 `MainActivity`
- **方法名**：小驼峰（camelCase），如 `showReminderDialog()`
- **常量**：全大写下划线，如 `TAG`, `CHANNEL_ID`
- **变量名**：小驼峰，如 `reminderManager`, `selectedDate`

### 2. 注释规范
- **类注释**：说明类的用途
- **方法注释**：说明方法的功能和参数
- **关键代码**：添加行内注释

### 3. 代码组织
- **按功能分组**：相关方法放在一起
- **私有方法在前**：公共方法在后
- **生命周期方法**：按顺序排列

---

## 🚀 部署指南

### 1. 生成签名密钥

```bash
keytool -genkey -v -keystore calendar-release-key.jks \
    -keyalg RSA -keysize 2048 -validity 10000 \
    -alias calendar
```

### 2. 配置签名

在 `app/build.gradle.kts` 中添加：
```kotlin
android {
    signingConfigs {
        create("release") {
            storeFile = file("calendar-release-key.jks")
            storePassword = "your-password"
            keyAlias = "calendar"
            keyPassword = "your-password"
        }
    }
    buildTypes {
        getByName("release") {
            signingConfig = signingConfigs.getByName("release")
            isMinifyEnabled = true
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
        }
    }
}
```

### 3. 构建 APK

```bash
./gradlew assembleRelease
```

### 4. 构建 AAB（Google Play）

```bash
./gradlew bundleRelease
```

---

## 📚 学习资源

### Android 官方文档
- [AlarmManager 指南](https://developer.android.com/training/scheduling/alarms)
- [通知指南](https://developer.android.com/develop/ui/views/notifications)
- [Material Design 3](https://m3.material.io/)

### 相关技术
- [Gson 文档](https://github.com/google/gson)
- [SharedPreferences 指南](https://developer.android.com/training/data-storage/shared-preferences)

---

## 📄 许可证

本项目仅供学习使用。

---

## 👥 贡献

欢迎提交 Issue 和 Pull Request！

---

## 📞 联系方式

如有问题，请提交 Issue 或联系开发者。

---

**最后更新**：2025年1月

**版本**：1.0.0

